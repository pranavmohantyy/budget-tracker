from dataclasses import dataclass\nfrom datetime import datetime\nimport json\nimport csv\nimport matplotlib.pyplot as plt\n\n@dataclass\nclass Transaction:\n    date: datetime\n    amount: float\n    category: str\n    description: str\n    type: str  # 'income' or 'expense'\n\n    def __post_init__(self):\n        if self.type not in ['income', 'expense']:  \n            raise ValueError('type must be either income or expense')\n\ntransactions = []\n\nbudgets = {}\n\ndef load_transactions():\n    global transactions\n    try:\n        with open('budget_data.json', 'r') as f:\n            data = json.load(f)\n            for item in data:\n                transactions.append(Transaction(**item))\n    except FileNotFoundError:\n        pass\n\ndef export_to_csv(filename):\n    with open(filename, 'w', newline='') as csvfile:\n        fieldnames = ['date', 'amount', 'category', 'description', 'type']\n        writer = csv.DictWriter(csvfile, fieldnames=fieldnames)\n\n        writer.writeheader()\n        for transaction in transactions:\n            writer.writerow(transaction.__dict__)\n\ndef display_monthly_spending():\n    monthly_spending = {}\n    for transaction in transactions:\n        month = transaction.date.strftime('%Y-%m')\n        if month not in monthly_spending:\n            monthly_spending[month] = {}\n        if transaction.category not in monthly_spending[month]:\n            monthly_spending[month][transaction.category] = 0\n        if transaction.type == 'expense':\n            monthly_spending[month][transaction.category] += transaction.amount\n\n    for month, categories in monthly_spending.items():\n        categories_list = list(categories.keys())\n        amounts = list(categories.values())\n        plt.bar(categories_list, amounts)\n        plt.title(f'Monthly Spending for {month}')\n        plt.xlabel('Categories')\n        plt.ylabel('Amount Spent')\n        plt.show()\n